// register use:
// r12 video memory base
// r5 snake head position
// r2 snake colour

// general setup
mov r12,#0x8000000		// video memory base in word mode (0x20000000)
add r5,r12,#272			// snake initial head position
mov r2,#0			//Body colour: 0 = black
			
//drawHead
str r2,[r5]				// draw snake head

halt
